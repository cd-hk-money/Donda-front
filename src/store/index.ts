import Vue from 'vue'
import Vuex from 'vuex'

import StockStore from './StockStore'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    StockStore
  }
})
